from Variables import Variables
import sqls as sqls
import datetime
from Logger import Logger
log = Logger()


def customer_load():

    print("Loading to Customer " + str(datetime.datetime.now()))
    log.log_message("Loading to Customer " + str(datetime.datetime.now()))

    
    

    print("Loaded to Customer " + str(datetime.datetime.now()))
    log.log_message("Loaded to Customer " + str(datetime.datetime.now()))
